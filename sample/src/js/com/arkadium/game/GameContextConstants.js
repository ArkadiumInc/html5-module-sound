/**
 * Created by jedi on 16-Feb-16.
 */
var GameContextConstants = function()
{
};

GameContextConstants.EntityNames = {
    TextureAtlasHelper: 'TextureAtlasHelper',
    PanelManager: 'PanelManager',
    EventBus: 'EventBus',
    StorableData: 'StorableData'
};

Object.freeze(GameContextConstants.EntityNames);

module.exports = GameContextConstants;
